name: Gainer Dump Detector

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests pandas numpy

      - name: Test Binance API connectivity
        run: |
          curl -s -o /dev/null -w "HTTP %{http_code}" https://fapi.binance.com/fapi/v1/ping || true
          curl -s -o /dev/null -w "HTTP %{http_code}" https://fapi1.binance.com/fapi/v1/ping || true

      - name: Run scanner
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          HTML_OUTPUT: 'docs/index.html'
        run: |
          mkdir -p docs
          python scan_dump.py

      - name: Deploy to GitHub Pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A docs/
          git diff --cached --quiet || {
            git commit -m "update dashboard $(date -u +'%Y-%m-%d %H:%M UTC')"
            git push
          }
